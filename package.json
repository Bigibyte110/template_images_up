{
  "name": "smart-template-generator",
  "version": "1.0.0",
  "description": "Smart Template Generator with AI Suggestions",
  "engines": {
    "node": "24.x",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "vercel-build": "echo 'Installing Playwright...' && npx playwright install chromium"
  },
  "dependencies": {
    "@playwright/browser-chromium": "^1.40.0"
  }
}
