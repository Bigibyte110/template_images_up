{
  "name": "smart-template-generator",
  "version": "1.0.0",
  "description": "Smart Template Generator with AI Suggestions",
  "engines": {
    "node": "18.x",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "postinstall": "playwright install chromium --with-deps"
  },
  "dependencies": {
    "playwright": "^1.40.0"
  }
}
