{
  "name": "smart-template-generator",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "playwright": "^1.40.0"
  }
}
